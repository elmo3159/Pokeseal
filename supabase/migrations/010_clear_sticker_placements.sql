-- =============================================
-- シール配置データをクリア
-- ユーザーが手動で再配置できるようにする
-- =============================================

-- すべてのシール配置を削除
DELETE FROM sticker_placements;

-- 確認メッセージ
DO $$
BEGIN
  RAISE NOTICE '✅ sticker_placements テーブルをクリアしました';
  RAISE NOTICE 'ユーザーはシールを手動で再配置できます';
END;
$$;
